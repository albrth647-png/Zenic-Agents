//! Re-export: RoleRegistry is defined in `manager.rs`.
//! The `Role` type used here is the same `Role` from `permissions.rs`,
//! re-exported through the parent module.

pub use super::manager::RoleRegistry;
